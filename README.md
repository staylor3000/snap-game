# Snap!

A browser-based Snap card game — player vs CPU.

## How to play

Open `index.html` in a browser. No build step or dependencies required.

- **Flip Card** (or `Space`) — flip your top card onto the pile
- **SNAP!** (or `Enter`) — call snap when the top two cards match
- First to collect all 52 cards wins
